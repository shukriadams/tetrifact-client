namespace TetrifactClient
{
    /// <summary>
    /// Event that passes a single string. What could be simpler, eh?
    /// </summary>
    /// <param name="data"></param>
    public delegate void SimpleEvent(string data);
}
